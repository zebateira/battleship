class Board
	types
		Houses = set of House;
	values
-- TODO Define values here
	instance variables
		size: int := 1;
		type: int := 0;
		houses: Houses;
		
		inv card(houses) >= 1;
		inv size >= 1 and type >= 0 and type <=3;
		
	operations
		private Board : int * int ==> Board
		Board (s, t) == 
						(size := s;
						type := t;);
						
	functions
-- TODO Define functiones here
	traces
-- TODO Define Combinatorial Test Traces here
end Board